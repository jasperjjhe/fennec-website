# Codex instructions

Read and follow [`AGENTS.md`](./AGENTS.md) for the canonical project guidance.

In particular, keep the site standalone: never introduce `fennecapp.com` or `app.fennecapp.com` links or remote image dependencies. Use the local assets, preserve complete screenshot frames, validate with `npm run build`, and check the GitHub Pages workflow after publishing.
